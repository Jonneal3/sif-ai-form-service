"""
Form step generation pipeline (Planner -> Renderer).

This package contains the end-to-end orchestrator plus a small set of orchestrator-only helpers
(payload parsing, context building, flow policy).
"""

__all__: list[str] = []


"""
Internal library package for sif-ai-form-service.

This package holds reusable building blocks (DSPy programs, schemas, utilities).
Request/response entrypoints live under `src/app/pipeline/` and the Vercel handler stays under `api/`.
"""

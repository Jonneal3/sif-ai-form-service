"""Type aliases for session log structures."""

from typing import Dict, Any

# Type aliases for session log structures
# These are essentially Dict[str, Any] but with semantic meaning
BatchSessionLog = Dict[str, Any]
FormSessionLog = Dict[str, Any]
